# 传感器公司门户网站建设原子级任务计划

## 一、页面 UI 设计与开发

- 1.1 实现首页页面，包括导航栏、Banner、公司简介、产品分类入口、应用场景推荐、页脚。
- 1.2 实现产品中心页面，包含分类导航、产品列表、筛选/搜索/分页功能。
- 1.3 实现产品详情页，支持大图/结构图预览、参数表横向滚动、应用场景推荐、资料下载。
- 1.4 实现应用场景列表与详情页。
- 1.5 实现关于我们、联系我们页面，集成在线留言表单。
- 1.6 实现响应式布局，适配移动端。
- 1.7 按视觉规范统一按钮、表格、卡片、字体、色彩等组件样式。

## 二、数据结构与内容管理

- 2.1 设计并实现产品数据 TypeScript interface 和 JSON Schema。
- 2.2 按照 schema 规范梳理并录入所有产品数据（JSON/MDX）。
- 2.3 设计应用场景数据结构并录入所有应用场景。
- 2.4 规范产品图片、结构图、资料等静态资源的命名与归档。
- 2.5 配置多语言（i18n）字段，补全中英文内容。

## 三、API 设计与实现

- 3.1 实现产品分类列表接口（GET /api/products/categories）。
- 3.2 实现产品列表接口，支持分页、筛选、模糊搜索（GET /api/products）。
- 3.3 实现产品详情接口（GET /api/products/[model]）。
- 3.4 实现应用场景列表接口（GET /api/applications）。
- 3.5 实现资料下载接口（GET /api/downloads/{file}）。
- 3.6 实现在线留言接口，集成验证码与频率限制（POST /api/contact）。
- 3.7 为所有接口补充 OpenAPI/Swagger 注释。
- 3.8 配置接口权限、错误码、日志记录。

## 四、自动化校验与 CI 集成

- 4.1 编写 Node.js 脚本，基于 ajv 校验所有产品数据的 JSON Schema 合规性。
- 4.2 实现图片、资料文件存在性校验。
- 4.3 实现多语言字段、参数单位、跨文件引用（如应用场景）校验。
- 4.4 输出详细校验报告（JSON/HTML）。
- 4.5 配置 GitHub Actions，实现数据/资源变更自动校验。

## 五、内容维护与协作流程

- 5.1 制定 PR 提交流程，要求每次数据/资源变更均通过 PR 并附校验报告。
- 5.2 指定审核人，配置自动化校验通过后方可合并。
- 5.3 配置自动部署到测试环境，人工验收后发布生产。
- 5.4 配置历史版本保留与一键回滚机制。
- 5.5 定期组织数据规范和内容维护培训。
- 5.6 通过 issue 跟踪内容优化和 bug 修复。

## 六、安全与合规性

- 6.1 对所有 POST/PUT/DELETE 接口实现鉴权。
- 6.2 在线留言接口集成验证码、频率限制，防止垃圾信息。
- 6.3 资料下载区仅开放公开资料，敏感资料需鉴权。
- 6.4 对用户/客户敏感信息展示进行脱敏处理。
- 6.5 配置 API 访问和数据变更日志。
- 6.6 公布隐私政策，确保合规（中国网络安全法、GDPR、CCPA 等）。
- 6.7 针对境外业务，补充本地化合规措施（如数据导出/删除、用户请求通道等）。

## 七、性能优化

- 7.1 配置图片 CDN 加速，优化图片加载速度。
- 7.2 对产品列表、详情等接口启用服务端缓存（如 Redis）。
- 7.3 前端实现图片、长列表懒加载。
- 7.4 启用静态资源压缩（图片、JS、CSS）。
- 7.5 优化 Next.js SSR/SSG 配置，提升首屏渲染速度。
- 7.6 接入前后端性能监控与预警系统。
